#include <stdio.h>
#include <math.h>

int main() {

	printf("The value of PI is %lf\n",M_PI);
	printf("The value of COS(PI) is %lf\n",cos(M_PI));
	printf("The sqrt(PI) is %lf\n",sqrt(M_PI));

	return 0;
}
